define([
  'pentaho/context!'
],
function(context) {

    // Obtain all registered visualization types.
    context
    .apply(['pentaho:visualization:IVisualizationType*'], function(vizTypes) {

        // TODO: Setup menu...

    });

});
